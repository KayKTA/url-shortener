{
    "name": "url-shortener",
    "private": true,
    "scripts": {
        "docker:up": "docker compose up -d",
        "docker:down": "docker compose down",

        "install:all": "npm --prefix backend install && npm --prefix frontend install",
        "setup": "npm run install:all && npm run docker:up",

        "dev": "concurrently -k -n backend,frontend -c blue,green \"npm --prefix backend run start:dev\" \"npm --prefix frontend run dev\"",
        "test:e2e": "npm --prefix backend run test:e2e"
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}
